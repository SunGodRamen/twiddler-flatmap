#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
echo "==> validate"
python3 "$ROOT/tools/validate-flatmap.py"
echo "==> render review"
python3 "$ROOT/tools/render-review.py"
echo "==> done"
